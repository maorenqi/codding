<view class="card-container">
    <view  class="bless-list" wx:for="{{blessContent}}" >
        <text class="bless-item">{{index + 1}}、{{item.content}}</text>
        <view bindtap="selBless" class="confirm" data-blessid="{{index}}">
            <text>确定</text>
        </view>
    </view>
</view>